# 2026-08-15 Term 1 快照复盘

## 更正结论

`2026-08-15T00-47-47+08-00` 快照包含 126 个 subject 请求文件，但完整性应拆开判断：

- 115 个 subject 成功取得排课数据。
- 11 个 subject 连续两次仅返回空白结果区，未出现明确的 no-results 文本；这些项目必须标记为待复核，不能视为确认无课。
- CHEM 与 CLCP 在旧快照分别存在 49、111 条班别组件，本轮却为空白。因此差异报告中的 160 条“消失”高度可能是本轮漏抓，而不是真实撤课。

## 新增与消失数字的正确含义

- `新增 1504` 是班别组件记录数，不是课程门数；对应 916 个 Class Nbr、914 个 Class Code，全部来自旧快照完全漏掉的 29 个 subject。
- `消失 161` 也是班别组件记录数：CHEM 49、CLCP 111、GPAD 1。
- CHEM 与 CLCP 共 160 条不能作为真实消失；GPAD3451（Class Nbr 6687）是唯一仍需单独核实的真实变化候选。
- 旧、新均存在的班别中，1314 条组件记录发生字段变化；其中 1312 条只改变 Vacancy，另 2 条同时改变 Teaching Staff。

## 后续规则

抓取器已调整：只有 CUSIS 明确显示 no-results 文本时才能标记 `confirmed_empty`；连续空白将标记 `needs_review`，并阻止快照被当作完整数据发布。
